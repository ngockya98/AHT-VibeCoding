{
  "title": "DB Slow Query",
  "panels": [
    {
      "type": "graph",
      "title": "Slow Queries",
      "targets": [
        {
          "expr": "sum(rate(pg_stat_activity_duration_seconds_bucket[5m])) by (le, service)",
          "legendFormat": "{{service}}"
        }
      ]
    }
  ]
}
