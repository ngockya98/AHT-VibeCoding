import { mockOnlineSavings } from './mockApi';

export const onlineSavings = async (amount: number, term: number) => {
  // TODO: Replace mockOnlineSavings with real Firebase logic
  return mockOnlineSavings(amount, term);
};
