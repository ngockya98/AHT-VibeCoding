import { mockVirtualCard } from './mockApi';

export const createVirtualCard = async (name: string) => {
  // TODO: Replace mockVirtualCard with real Firebase logic
  return mockVirtualCard(name);
};
