import { mockGetTransactions, mockResetTransactions } from './mockApi';

export const getTransactions = async () => {
  // TODO: Replace mockGetTransactions with real Firebase logic
  return mockGetTransactions();
};

export const resetTransactions = async () => {
  // TODO: Replace mockResetTransactions with real Firebase logic
  return mockResetTransactions();
};
